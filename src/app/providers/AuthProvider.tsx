// "use client";
// import { SessionProvider } from "next-auth/react";

// export default function AuthProvider({ session, children }) {
//   return (
//     <SessionProvider refetchInterval={2 * 60} session={session}>
//       {children}
//     </SessionProvider>
//   );
// }
