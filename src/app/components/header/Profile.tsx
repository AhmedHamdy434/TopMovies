const Profile = () => {
  return (
    <button
      className="px-[14px] py-[6px] border-0 rounded-[5px]
      cursor-pointer bg-background"
    >
      Sign In
    </button>
  );
};

export default Profile;
