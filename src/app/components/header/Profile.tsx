const Profile = () => {
  return (
    <>
      <button
        className="text-[16px] px-[14px] py-[6px] border-0 rounded-[5px]
      cursor-pointer bg-background text-foreground hover:bg-[var(--btn-background-hover)]
      hover:text-[var(--btn-color-hover)] transition-all duration-300"
      >
        Sign In
      </button>
    </>
  );
};

export default Profile;
